{
  "name": "shikaka",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "sh -c 'if [ -x .venv/bin/python ]; then .venv/bin/python server.py; else python3 server.py; fi'",
    "start:node": "node server.js",
    "check": "python3 -m py_compile server.py && node --check public/app.js && node --check server.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=18"
  }
}
